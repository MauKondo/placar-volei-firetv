# Copie este arquivo para secrets.properties (não versionado) e ajuste o PIN.
# secrets.properties está no .gitignore — seguro pra colocar PIN real.
#
# Comando:
#   cp secrets.properties.example secrets.properties
#   (Windows PowerShell: Copy-Item secrets.properties.example secrets.properties)
#
# Depois edite PIN para algo só você sabe. Compartilhe verbalmente com quem
# precisa controlar o placar.

PIN=1234
